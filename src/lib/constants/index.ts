import * as EVENTS from './events';
import * as ROUTES from './routes';
import * as ERRORS from './errors';

const MYNEWS = 'mynews';
const LANGUAGE = 'en';

export { EVENTS, ROUTES, ERRORS, MYNEWS, LANGUAGE }
